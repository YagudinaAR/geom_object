#pragma once
#include"IPoint.h"
#include"Ishape.h"
#include"ILine.h"
/*struct unit
{
	ILine link;
	unit *l;
	unit *r;
};
class IRoot
{
public:
	unit *h;
	IRoot()
	{
		h = NULL;
	}
	void addpoint(Ipoint p)
	{
		if (h == NULL)
		{
			h->link = p;
		}
		else
		{

		}
	}
};*/